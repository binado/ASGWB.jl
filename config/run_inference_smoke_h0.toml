cache_path = "../analysis_numpyro_julia_cache.h5"
detectors = ["S1", "R1"]
sample_only = ["H0"]
seed = 1

output_dir = "../logs/smoke"
output_prefix = "smoke"

[sampler]
n_samples = 200
n_adapts = 100
target_acceptance = 0.8
num_chains = 2
checkpoint_every = 50
