cache_path = "../ASGWBInference/deps/parity_precompile.h5"
detectors = ["H1", "L1"]
sample_only = ["H0"]
seed = 1

output_dir = "../ASGWBInference/build/precompile-run"
output_prefix = "precompile"

[sampler]
n_samples = 1
n_adapts = 1
target_acceptance = 0.8
num_chains = 1
checkpoint_every = 0
